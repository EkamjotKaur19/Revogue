import {createMuiTheme} from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#764abc"
    },
    secondary: {
      main: "#af52bf"
    },
  },
  background: {
    default: "#ffffff"
  } 
});

export default theme;